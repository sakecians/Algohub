// 3 col
// 1 - explaination
// 2 - algorithm
// 3 - application

export const merge = {
    explaination: {
        exp: "Merge sort is a sorting technique based on divide and conquer technique. Merge sort first divides the array into equal halves and then combines them in a sorted manner. The merge() function is used for merging two halves. Merge sort keeps on dividing the list into equal halves until there is only a single element left in the list. The single element in the list is considered as sorted list. Merge sort combines the smaller sorted list and enters into the new list in sorted order.",
        timeComplexity: {
            bestCase: 'O(n log n)',
            avgCase: 'O(n log n)',
            worstCase: 'O(n log n)',
        },
        spaceComplexity: 'O(n)',
        algorithm: [
            'Break up the array into halves until you have arrays that are empty or have one element.',
            'Once you have smaller sorted arrays, merge those arrays with other sorted arrays until you are back at the full length of the array.',
            'Once the array has been merged back together, return the merged and sorted array',
        ],
    }
}

export const bubble = {
    explaination: {
        exp: "Bubble sort is a sorting technique based on divide and conquer technique. Merge sort first divides the array into equal halves and then combines them in a sorted manner. The merge() function is used for merging two halves. Merge sort keeps on dividing the list into equal halves until there is only a single element left in the list. The single element in the list is considered as sorted list. Merge sort combines the smaller sorted list and enters into the new list in sorted order.",
        timeComplexity: {
            bestCase: 'O(n log n)',
            avgCase: 'O(n log n)',
            worstCase: 'O(n log n)',
        },
        spaceComplexity: 'O(n)',
        algorithm: [
            'Break up the array into halves until you have arrays that are empty or have one element.',
            'Once you have smaller sorted arrays, merge those arrays with other sorted arrays until you are back at the full length of the array.',
            'Once the array has been merged back together, return the merged and sorted array',
        ],
    }
}

export const quick = {
    explaination: {
        exp: "Quick sort is a sorting technique based on divide and conquer technique. Merge sort first divides the array into equal halves and then combines them in a sorted manner. The merge() function is used for merging two halves. Merge sort keeps on dividing the list into equal halves until there is only a single element left in the list. The single element in the list is considered as sorted list. Merge sort combines the smaller sorted list and enters into the new list in sorted order.",
        timeComplexity: {
            bestCase: 'O(n log n)',
            avgCase: 'O(n log n)',
            worstCase: 'O(n log n)',
        },
        spaceComplexity: 'O(n)',
        algorithm: [
            'Break up the array into halves until you have arrays that are empty or have one element.',
            'Once you have smaller sorted arrays, merge those arrays with other sorted arrays until you are back at the full length of the array.',
            'Once the array has been merged back together, return the merged and sorted array',
        ],
    }
}

export const radix = {
    explaination: {
        exp: "Radix sort is a sorting technique based on divide and conquer technique. Merge sort first divides the array into equal halves and then combines them in a sorted manner. The merge() function is used for merging two halves. Merge sort keeps on dividing the list into equal halves until there is only a single element left in the list. The single element in the list is considered as sorted list. Merge sort combines the smaller sorted list and enters into the new list in sorted order.",
        timeComplexity: {
            bestCase: 'O(n log n)',
            avgCase: 'O(n log n)',
            worstCase: 'O(n log n)',
        },
        spaceComplexity: 'O(n)',
        algorithm: [
            'Break up the array into halves until you have arrays that are empty or have one element.',
            'Once you have smaller sorted arrays, merge those arrays with other sorted arrays until you are back at the full length of the array.',
            'Once the array has been merged back together, return the merged and sorted array',
        ],
    }
}